# Get a token at https://replicate.com/account
# Used only on the server (Vite dev middleware + Vercel api/replicate.js)
REPLICATE_API_TOKEN=

# Optional overrides (defaults shown in replicate-handlers.js)
# REPLICATE_TEXT_MODEL=meta/meta-llama-3-8b-instruct
# REPLICATE_IMAGE_MODEL=black-forest-labs/flux-2-pro
